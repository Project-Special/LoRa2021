# Nuvem OPCIONAL. Sem isto o app funciona por completo, gravando as campanhas
# no proprio aparelho (IndexedDB). Preencha so para consolidar campanhas de
# varios aparelhos e comparar bandas depois.
#
# Project Settings -> API no dashboard do Supabase.
# Rode supabase-schema.sql antes.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
