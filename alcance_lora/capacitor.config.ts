import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.alfatronic.alcancelora',
  appName: 'Alcance LoRa',
  webDir: 'dist',
};

export default config;
